import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    color: '#000',
    fontSize: 16,
    padding: 10,
    marginBottom: 100
  },
  subtotalContainer: {
    marginVertical: 10,
  },
  subtotal: {
    color: '#000',
  },
});

export default styles;
