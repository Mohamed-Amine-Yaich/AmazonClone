import {View, Text} from 'react-native';
import React from 'react';

const MoreScreen = () => {
  return (
    <View style={{justifyContent: 'center', alignItems: 'center', flex: 1}}>
      <Text style={{color: '#000'}}>MoreScreen</Text>
    </View>
  );
};

export default MoreScreen;
